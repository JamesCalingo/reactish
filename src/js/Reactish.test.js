// const Reactish = require("./Reactish")

it("Creates HTML elements and adds them to the DOM", () => {

})

it("returns object", () => {
  // const element = createElement("div", null, "CHILDREN")
  // expect(typeof element).toBe("object")
})

it("creates a text element from string/text", () => {

})

it("creates a DOM from the fibers", () => {
  
})

it("removes old DOM properties and events and inserts new DOM properties and events", () => {

})